# Passed from stage2:
# ---------------------------------------------------------
# set -e +a +m +s +i -f
# . /start/stage2.functions
# isFirstRun

. /start/stage3.functions

if [ "$isFirstRun" == "true" ]
then
   initPgadmin
fi
if [ "$(isInsecure)" == "true" ]
then
   printSecurityWarning
fi
